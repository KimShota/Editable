node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Pipeline: downloaded whisper models, per-job artifacts, staged job assets
models
artifacts
public/jobs

# Format-authoring: downloaded reference clips, sampled frames, drafts
# (root-anchored — src/backend/authoring/ and src/app/authoring/ are source code)
/authoring

# Next.js app
.next
next-env.d.ts
tsconfig.tsbuildinfo

# App-managed data (jobs created through the UI, user's asset library)
jobs/*
!jobs/demo
!jobs/five-codes
!jobs/five-codes-demo
library
